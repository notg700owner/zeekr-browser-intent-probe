name = "zeekr-browser-intent-probe"
compatibility_date = "2026-04-29"
main = "./src/worker.js"

# Static assets are served from `public`; `/api/*` is handled by `src/worker.js`.
[assets]
directory = "./public"
binding = "STATIC_ASSETS"
run_worker_first = ["/api/*"]
