{
  "name": "zeekr-browser-intent-probe",
  "version": "1.6.0",
  "private": true,
  "description": "Defensive Chromium capability probe for authorised Zeekr rear-screen browser assessments.",
  "scripts": {
    "build": "node -e \"console.log('Static Cloudflare Pages site: public directory is ready')\"",
    "preview": "npx wrangler dev",
    "deploy": "npx wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.14.4"
  }
}
